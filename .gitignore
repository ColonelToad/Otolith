# pixi (env is reproducible from pixi.lock)
.pixi/*
!.pixi/config.toml

# colcon / CMake / C++
build/
install/
log/
fusion/build/
*.o
*.so

# Rust
target/

# Python
__pycache__/
*.pyc
.venv/

# MuJoCo Menagerie is reused from ../rally via symlink (see CLAUDE.md)
third_party/menagerie

# eval outputs
eval/out/

# raw screen-capture takes (only assets/Go2Demo.mp4 + assets/go2_demo.gif ship)
/*.mp4

# Windows download markers (Zone.Identifier ADS streams)
/*.Zone.Identifier

# HDL scratch + Verilator build outputs + synth/P&R build dirs
hdl/work/
hdl/tb/obj_dir/
hdl/synth/build/
*.vcd
*.gtkw

# OS
.DS_Store

# personal notes (not part of the repo)
notes/
session-*.md
